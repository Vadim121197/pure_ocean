import { ObjectTypeGeneric } from '../types';

export const routes: ObjectTypeGeneric<string> = {
  MAIN: '/',
  HISTORY: '/history',
  DOCS: '/docs',
  SHARE: '/share',
  REWARD: '/reward',
  BOND: '/bond',
};
